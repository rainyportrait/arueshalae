*src/userscript-legacy/*

This directory is LEGACY code from a previous implementation. It has NO relevance to the current codebase and should NEVER be read, modified, or used unless explicitly told to do so with the exact path `src/userscript-legacy/`.

The current userscript (bundled code) is built from `src/ui/index.ts` via `build-userscript.ts`. 
